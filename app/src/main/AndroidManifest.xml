<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.electricityproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".ui.activity.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.MyFootActivity"></activity>
        <activity android:name=".ui.activity.NewAddressActivity" />
        <activity android:name=".ui.activity.MyMoneyActivity" />
        <activity android:name=".ui.activity.MyCircleActivity" />
        <activity android:name=".ui.activity.MydataActivity" />
        <activity android:name=".ui.activity.SubmitOrderActivity" />
        <activity android:name=".ui.activity.AddressActivity" />
        <activity android:name=".ui.activity.SureActivity" />
        <activity android:name=".ui.activity.Search2Activity" />
        <activity android:name=".ui.activity.SearchActivity" />
        <activity android:name=".ui.activity.DetailsActivity" />
        <activity android:name=".ui.activity.HomeActivity" />
        <activity android:name=".ui.activity.RegisterActivity" />
        <activity android:name=".ui.activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>